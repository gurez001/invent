import Vendor from '@/components/pages/crm/vendor/Vendor';
import React from 'react'
export const metadata = { title: `Vendor | List` };
const page = () => {
  return (
   <Vendor/>
  )
}

export default page;
